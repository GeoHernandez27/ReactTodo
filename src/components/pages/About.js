import React from 'react'

export default function About() {
    return (
        <React.Fragment>
            <h1>About</h1>
            <p>To do list built using React and React-Router. API calls to jsonplaceholder made with axios.</p>
        </React.Fragment>
    )
}
